# Compiler files
cache/
out/

# Ignore all broadcast artifacts (do not upload broadcast logs / run-latest json)
broadcast/
# keep exceptions (if any) remove the following lines if present
!/broadcast/*.keep

# Dev-only dry-run / local network folders
/broadcast/*/31337/
/broadcast/**/dry-run/

# Build artifacts from forge/hardhat/truffle
/bin/
/bin/**

# common build artifact patterns (if bin contains mixed files)
bin/**/*.abi
bin/**/*.bin
bin/**/*.json

# Forge/Foundry cache & artifacts
/.forge/
/lib/             # if you vendor libs locally

# Docs
docs/

# Dotenv file
.env

# OS / editor
.DS_Store
.vscode/
.idea/
